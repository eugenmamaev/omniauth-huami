require 'spec_helper'

describe Omniauth::Huami do
  it 'has a version number' do
    expect(Omniauth::Huami::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
